@model List<Aventure>

@{
    ViewData["Title"] = "Aventure Page";
    string aventureTitle = ViewBag.Titre;
}

<h1 class="text-center">@aventureTitle</h1>

<div class="row justify-content-center">
    <a asp-controller="Aventure" asp-action="Create" class="btn btn-primary">Nouvelle Aventure</a>
</div>

<h2 class="text-center small">Dernières aventures</h2>
<div class="row">
    @foreach (Aventure aventure in Model)
    {
        <a href="#" class="card border m-2 text-center" style="width: 7rem;">
            <div class="card-body">@aventure.Titre</div>
        </a>
    }
</div>