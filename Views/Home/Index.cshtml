@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    @foreach (int item in ViewBag.monTableau)
    {
        <a href="#" class="card border m-2 text-center" style="width: 7rem;">
            <div class="card-body">Mon contenu</div>
        </a>
    }
</div>
